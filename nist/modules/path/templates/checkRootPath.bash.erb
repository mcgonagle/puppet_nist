#!/bin/bash

if [[ -n $( /bin/echo $PATH | grep -E "^:|::|:$" ) ]]; then
	/bin/echo "root's PATH should not have an empty element in it" 
fi

if [[ -n $( /bin/echo $PATH | grep -E "^[.]|:[.]+:|[.]$" ) ]]; then
	/bin/echo "root's PATH should not have a dot in it" 
fi

